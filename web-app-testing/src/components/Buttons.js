import React from "react";

const Buttons = (props) => {
    return (
        <div>
            <button>Strike!</button>
            <button>Ball!</button>
            <button>Foul!</button>
        </div>
    );
}

export default Buttons;